'use client'; import {motion} from 'framer-motion'; import {Shield,Droplet,Sparkles,Sun,Gauge,Wind,Settings,Repeat,ArrowUpRight} from 'lucide-react'; import {services} from '@/data/site'; import Reveal from '@/components/ui/Reveal'; const icons={shield:Shield,droplet:Droplet,sparkles:Sparkles,sun:Sun,gauge:Gauge,wind:Wind,settings:Settings,repeat:Repeat}; export default function ServicesPage(){return <section className="section services-page"><div className="container"><Reveal><div className="section-head narrow"><span className="eyebrow">SERVICES</span><h2>Care that comes<br/><i>to your vehicle.</i></h2><p>Explore Baker’s core services. Pricing is quote-based in the available public information, so this demo keeps the focus on outcomes rather than made-up packages.</p></div></Reveal><div className="service-grid">{services.map((s,i)=>{const Icon=icons[s.icon as keyof typeof icons];return <Reveal key={s.title} delay={i*.03}><article id={s.title.toLowerCase().replaceAll(' ','-')} className="service-card"><div className="service-number">{s.k}<Icon size={21}/></div><h3>{s.title}</h3><p>{s.short}</p><a href="/contact">Request a quote <ArrowUpRight size={15}/></a></article></Reveal>})}</div></div></section>}
