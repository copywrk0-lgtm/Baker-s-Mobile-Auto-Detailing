'use client';
import Image from 'next/image'; import {motion} from 'framer-motion'; import Reveal from '@/components/ui/Reveal'; import {gallery} from '@/data/site';
export default function GalleryPage(){return <section className="section dark-section gallery-page"><div className="container"><Reveal><div className="showcase-head"><div><span className="eyebrow">THE WORK</span><h2>Real cars.<br/><i>Real atmosphere.</i></h2></div><p>Built around the vehicle photography supplied from Baker’s Mobile Auto Detailing’ public Maps presence, so the experience feels specific to the shop.</p></div></Reveal><div className="gallery gallery-full">{gallery.map((g,i)=><Reveal key={g[1]} delay={i*.04}><motion.figure whileHover={{scale:.985}}><div className="image-wrap"><Image src={g[0]} alt={g[1]} fill sizes="(max-width: 800px) 100vw, 50vw"/></div><figcaption><div><strong>{g[1]}</strong><span>{g[2]}</span></div><span className="circle">↗</span></figcaption></motion.figure></Reveal>)}</div></div></section>}
