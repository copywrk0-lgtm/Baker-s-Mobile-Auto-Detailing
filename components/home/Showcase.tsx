'use client'; import Image from 'next/image'; import {motion} from 'framer-motion'; import {ArrowUpRight} from 'lucide-react'; import {gallery} from '@/data/site'; import Reveal from '@/components/ui/Reveal'; export default function Showcase(){return <section id="work" className="section dark-section"><div className="container"><Reveal><div className="showcase-head"><div><span className="eyebrow">THE WORK</span><h2>Your vehicle.<br/><i>Back to its best.</i></h2></div><p>A visual-first section designed to showcase Baker’s real detailing work. The demo uses clearly marked concept imagery until approved business photos are added.</p></div></Reveal><div className="gallery">{gallery.map((g,i)=><Reveal key={g[1]} delay={i*.05}><motion.figure whileHover={{scale:.985}}><div className="image-wrap"><Image src={g[0]} alt={g[1]} fill sizes="(max-width: 800px) 100vw, 50vw"/></div><figcaption><div><strong>{g[1]}</strong><span>{g[2]}</span></div><span className="circle">↗</span></figcaption></motion.figure></Reveal>)}</div></div></section>}
