'use client'; import {useState} from 'react'; import {Plus} from 'lucide-react'; import Reveal from '@/components/ui/Reveal'; const faqs=[['Where does Baker’s provide mobile service?','Baker’s describes service at your home, office or place of recreation. Confirm your exact location when requesting a quote.'],['How much does detailing cost?','Pricing is not published in the available business listings, so the demo intentionally avoids inventing package prices. Request a quote for the vehicle and service you need.'],['What vehicles can Baker’s handle?','Corey lists cars, boats, planes, semi trucks and RVs among the vehicles he services.'],['Do you offer recurring maintenance?','Yes. Baker’s lists weekly and bi-weekly maintenance services for customers who want to keep a vehicle consistently maintained.'],['How do I get started?','Call Baker’s directly or use the quote form to send the basic vehicle and service details through WhatsApp.']]; export default function FAQ(){const[active,setActive]=useState(0);return <section id="faq" className="section faq"><div className="container faq-grid"><Reveal><div><span className="eyebrow">FAQ</span><h2>Make the first<br/><i>decision easy.</i></h2></div></Reveal><div>{faqs.map((f,i)=><Reveal key={f[0]} delay={i*.03}><button className={`faq-item ${active===i?'active':''}`} onClick={()=>setActive(active===i?-1:i)}><span>{f[0]}</span><Plus size={18}/>{active===i&&<p>{f[1]}</p>}</button></Reveal>)}</div></div></section>}
