'use client'; import {motion} from 'framer-motion'; import {Shield,Droplet,Sparkles,Sun,Gauge,Wind,Settings,Repeat,ArrowUpRight} from 'lucide-react'; import {services} from '@/data/site'; import Reveal from '@/components/ui/Reveal'; const icons={shield:Shield,droplet:Droplet,sparkles:Sparkles,sun:Sun,gauge:Gauge,wind:Wind,settings:Settings,repeat:Repeat}; export default function Services(){return <section id="services" className="section"><div className="container"><Reveal><div className="section-head"><span className="eyebrow">THE SERVICES</span><h2>Expert care.<br/><i>Wherever your vehicle is.</i></h2><p>From routine detailing to restoration and protection, Baker’s brings the service to you — at home, at work or wherever the vehicle is parked.</p></div></Reveal><div className="service-grid">{services.map((s,i)=>{const Icon=icons[s.icon as keyof typeof icons];return <Reveal key={s.title} delay={i*.03}><motion.article className="service-card" whileHover={{y:-8}}><div className="service-number">{s.k}<Icon size={21}/></div><h3>{s.title}</h3><p>{s.short}</p><a href="#quote">Ask about it <ArrowUpRight size={15}/></a></motion.article></Reveal>})}</div></div></section>}
